# references:
# * http://www.objc.io/issue-6/travis-ci.html
# * https://github.com/supermarin/xcpretty#usage

language: objective-c

# cache: cocoapods
podfile: Example/Podfile

before_install:
  - brew install swiftlint
  - gem install cocoapods

osx_image:
  - xcode8
# env:
#   matrix:
#     - TASK=build
#     - TASK=lintpod

matrix:
    include:
        # Swift 3.0 Demo+Library
        - os: osx
          osx_image: xcode8
          env:
            - TASK=build

        # Swift 3.0
        - os: osx
          osx_image: xcode8
          env:
            - TASK=lintpod

script:
  - rake ci:$TASK
